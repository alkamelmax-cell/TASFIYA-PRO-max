# 🔄 إعادة ضبط تسلسل طلبات التصفية

## 📋 الوصف
أدوات لإعادة ضبط التسلسل التلقائي (Auto-increment) لأرقام طلبات التصفية في النظام.

---

## 🚀 الاستخدام

### الطريقة الموصى بها: ⭐

1. **شغّل التطبيق أولاً**:
   ```bash
   npm run dev
   ```

2. **في نافذة PowerShell جديدة، شغّل السكريبت**:
   ```bash
   node reset-sequence-api.js
   ```

### النتيجة:
- ✅ يتم إعادة ضبط التسلسل
- ✅ الطلب التالي سيبدأ من **#1**
- ✅ يعمل على SQLite و PostgreSQL

---

## 🛠️ الملفات المتاحة

### 1. `reset-sequence-api.js` (السكريبت الرئيسي)
- يستخدم API للاتصال بالسيرفر
- آمن وسريع
- لا يحتاج لمكتبات إضافية

### 2. `reset-requests-sequence.sql` (للمرجع)
- سكريبت SQL يمكن تنفيذه مباشرة على قاعدة البيانات
- مفيد للمطورين

---

## 🔌 API Endpoint

يمكنك أيضاً استخدام API مباشرة:

```bash
POST http://localhost:4000/api/reconciliation-requests/reset-sequence
```

مثال باستخدام `curl`:
```bash
curl -X POST http://localhost:4000/api/reconciliation-requests/reset-sequence
```

---

## ⚠️ ملاحظات مهمة

- يجب تشغيل التطبيق قبل استخدام `reset-sequence-api.js`
- العملية آمنة ولا تحذف أي بيانات موجودة
- فقط يعيد ضبط التسلسل لطلبات **جديدة**

---

## 📞 الدعم

إذا واجهت أي مشاكل، تأكد من:
1. ✅ التطبيق يعمل على المنفذ 4000
2. ✅ لديك صلاحيات الكتابة على قاعدة البيانات
3. ✅ قاعدة البيانات غير مقفلة

---

**تم التحديث**: 2026-02-02
