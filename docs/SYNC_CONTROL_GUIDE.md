# 🔄 دليل استخدام التحكم في المزامنة مع تطبيق الويب

## الموقع
يوجد زر التحكم في المزامنة في:
```
الإعدادات ⚙️ > معلومات النظام ℹ️ > قسم المزامنة مع تطبيق الويب 🔄
```

## الوظائف المتاحة

### 1. عرض حالة المزامنة
- **نشطة ✅**: المزامنة تعمل والبيانات تُرسل تلقائياً كل 30 ثانية
- **متوقفة ⏸️**: المزامنة موقفة مؤقتاً ولا يتم إرسال بيانات

### 2. التحكم في المزامنة
#### تفعيل المزامنة
- اضغط على زر "▶️ تفعيل المزامنة" (الزر الأخضر)
- سيتم بدء المزامنة التلقائية فوراً
- البيانات ستُرسل إلى تطبيق الويب كل 30 ثانية

#### إيقاف المزامنة مؤقتاً
- اضغط على زر "⏸️ إيقاف المزامنة" (الزر الأصفر)
- سيتم إيقاف المزامنة فوراً
- لن يتم إرسال أي بيانات حتى تُعيد تفعيلها
- **ملاحظة**: البيانات المحلية آمنة ولن تتأثر

### 3. التحديث التلقائي
- تتحدث حالة المزامنة تلقائياً كل 30 ثانية
- عند فتح تبويب "معلومات النظام" يتم تحديث الحالة فوراً

## متى تحتاج لإيقاف المزامنة؟

### حالات يُنصح بإيقاف المزامنة:
1. **مشاكل في الإنترنت**: إذا كان الاتصال بالإنترنت بطيء أو متقطع
2. **توفير الموارد**: عند العمل على مهام تحتاج موارد كبيرة من النظام
3. **الخصوصية المؤقتة**: إذا كنت تعمل على بيانات حساسة ولا تريد إرسالها للويب حالياً
4. **الصيانة**: عند إجراء صيانة على تطبيق الويب

### حالات يجب تفعيل المزامنة:
1. **العمل الجماعي**: لضمان رؤية الفريق لأحدث البيانات على تطبيق الويب
2. **النسخ الاحتياطي السحابي**: للحفاظ على نسخة من البيانات على السيرفر
3. **المتابعة عن بُعد**: لمتابعة العمليات من أي مكان عبر تطبيق الويب

## الأمان

✅ **آمن تماماً**: 
- إيقاف المزامنة لن يؤثر على بياناتك المحلية أبداً
- الإعداد يُحفظ في قاعدة البيانات ويبقى بعد إعادة تشغيل التطبيق
- يمكنك التفعيل والإيقاف متى شئت بدون مخاطر

## الدعم الفني

إذا واجهت أي مشكلة في المزامنة:
1. تحقق من اتصال الإنترنت
2. تحقق من أن تطبيق الويب يعمل بشكل صحيح
3. أعد تشغيل التطبيق إذا لزم الأمر
4. راجع سجل الأحداث (Console) للمزيد من التفاصيل

---
**تم التطوير بواسطة**: محمد أمين الكامل  
**التاريخ**: 2025  
**الإصدار**: 4.0.0
