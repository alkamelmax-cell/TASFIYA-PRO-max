# 📋 تعليمات اختبار حفظ واسترجاع إعدادات الطابعة الحرارية

## ✅ الحل المُطبّق

تم إصلاح مشكلة عدم حفظ إعدادات الطابعة الحرارية عند إغلاق وإعادة تشغيل التطبيق من خلال:

### 1. **حفظ الإعدادات في قاعدة البيانات**
   - عند النقر على "حفظ الإعدادات"، يتم حفظها في جدول `system_settings`
   - الفئة المستخدمة: `thermal_printer`
   - العناصر المحفوظة:
     - `fontName` - نوع الخط
     - `fontSize` - حجم الخط (رقم)
     - `copies` - عدد النسخ (رقم)
     - `color` - الطباعة الملونة (true/false)
     - `printerName` - اسم الطابعة
     - `paperWidth` - عرض الورقة (رقم)

### 2. **استرجاع الإعدادات عند بدء التطبيق**
   - عند تشغيل التطبيق، يتم تحميل الإعدادات المحفوظة تلقائياً
   - يتم تحويل قيم النصوص إلى الأنواع الصحيحة:
     - "true" / "false" ← boolean
     - "12" ← number (12)
     - "Courier New" ← string

### 3. **الملفات المُعدّلة**

#### `src/main.js`
- تحسين معالج `thermal-printer-settings-get`:
  - الآن يقرأ من قاعدة البيانات أولاً
  - يحول الأنواع بشكل صحيح
  - يعود إلى القيم المحفوظة بالذاكرة كخطة بديلة

- إضافة تحميل الإعدادات في `app.whenReady()`:
  - بعد 500 ملي ثانية من بدء التطبيق
  - ينتظر جاهزية قاعدة البيانات
  - يحمّل جميع الإعدادات المحفوظة

---

## 🧪 خطوات الاختبار العملية

### **الخطوة 1: تشغيل التطبيق الأول**
```powershell
npm start
```

### **الخطوة 2: الذهاب إلى إعدادات الطابعة الحرارية**
1. انقر على أيقونة ⚙️ (الإعدادات) في الشريط العلوي
2. اختر تبويب **"الطابعة الحرارية"** من الجانب الأيسر
3. ستظهر نموذج بالقيم الافتراضية:
   - نوع الخط: Courier New
   - حجم الخط: 10
   - عدد النسخ: 1
   - الطابعة: (اختيارية)

### **الخطوة 3: تغيير الإعدادات**
قم بتغيير القيم إلى:
- **نوع الخط**: Arial (أو أي خط آخر)
- **حجم الخط**: 12
- **عدد النسخ**: 2
- **الطابعة**: اختر "RONGTA 80mm Series Printer" (أو أي طابعة متاحة)
- **الطباعة الملونة**: علّم الخيار ✓

### **الخطوة 4: حفظ الإعدادات**
1. انقر على زر **"حفظ الإعدادات"**
2. يجب أن ترى رسالة نجاح: **"تم حفظ إعدادات الطابعة الحرارية بنجاح"**
3. تحقق من لوحة التحكم (DevTools) - يجب أن ترى:
   ```
   ✅ [THERMAL-PRINTER] تم حفظ الإعدادات في قاعدة البيانات
   ```

### **الخطوة 5: إغلاق التطبيق**
```powershell
# الطريقة 1: من واجهة التطبيق
# - انقر على زر الإغلاق (X)

# الطريقة 2: من PowerShell (إذا تعثر الإغلاق)
Get-Process electron | Stop-Process -Force
# أو
taskkill /IM electron.exe /F
```

### **الخطوة 6: إعادة تشغيل التطبيق**
```powershell
npm start
```

### **الخطوة 7: التحقق من استرجاع الإعدادات**
1. افتح إعدادات الطابعة الحرارية مجدداً
2. **يجب أن ترى القيم المحفوظة**:
   - نوع الخط: Arial (القيمة المحفوظة)
   - حجم الخط: 12 (القيمة المحفوظة)
   - عدد النسخ: 2 (القيمة المحفوظة)
   - الطابعة: RONGTA... (القيمة المحفوظة)
   - الطباعة الملونة: مفعّلة ✓ (القيمة المحفوظة)

3. تحقق من لوحة التحكم (DevTools):
   ```
   ✅ [THERMAL-PRINTER] تم تحميل الإعدادات المحفوظة من قاعدة البيانات
   ```

---

## ✔️ معايير النجاح

| الاختبار | النتيجة المتوقعة | الحالة |
|---------|-----------------|--------|
| حفظ الإعدادات | رسالة نجاح تظهر | ✅ |
| إعادة تشغيل التطبيق | الإعدادات تبقى محفوظة | ✅ |
| اسم الطابعة يظهر | القيمة المحفوظة تظهر | ✅ |
| حجم الخط محفوظ | القيمة المحفوظة (12) تظهر | ✅ |
| عدد النسخ محفوظ | القيمة المحفوظة (2) تظهر | ✅ |
| حالة اللون محفوظة | الخيار يبقى مفعّلاً | ✅ |

---

## 🔧 استكشاف الأخطاء

### المشكلة: الإعدادات تُحذف عند إعادة التشغيل

**الحل**:
1. تأكد من أن قاعدة البيانات موجودة:
   ```
   C:\Users\KC\AppData\Roaming\Casher\casher.db
   ```
2. تحقق من الأخطاء في لوحة التحكم (DevTools):
   - اضغط `F12` لفتح DevTools
   - ابحث عن رسائل الخطأ الحمراء

### المشكلة: قيم غير صحيحة (مثل "true" بدلاً من true)

**الحل**:
- تم حل هذه المشكلة بإضافة تحويل الأنواع في معالج `thermal-printer-settings-get`
- الآن يتم تحويل "true" إلى true تلقائياً

### المشكلة: الطابعة المختارة لا تظهر

**الحل**:
1. تأكد من وجود طابعات في النظام:
   ```powershell
   Get-Printer | Select-Object Name, Status
   ```
2. إذا لم توجد طابعات، حمّل قائمة الطابعات:
   - انقر على زر "تحديث الطابعات" 🔄 في الإعدادات

---

## 📊 التحقق من محتويات قاعدة البيانات

لعرض الإعدادات المحفوظة مباشرة في قاعدة البيانات:

```powershell
# استخدم أي أداة SQLite (مثل DB Browser for SQLite)
# أو شغّل الاستعلام التالي:

SELECT category, setting_key, setting_value 
FROM system_settings 
WHERE category = 'thermal_printer' 
ORDER BY setting_key;
```

**النتيجة المتوقعة**:
```
category         | setting_key  | setting_value
-----------------|--------------|-----------------------
thermal_printer  | color        | true
thermal_printer  | copies       | 2
thermal_printer  | fontName     | Arial
thermal_printer  | fontSize     | 12
thermal_printer  | paperWidth   | 80
thermal_printer  | printerName  | RONGTA 80mm Series Printer
```

---

## 📝 ملاحظات مهمة

- ✅ الإعدادات محفوظة في **قاعدة البيانات** وليس في ملف نصي
- ✅ يتم تحويل الأنواع بشكل صحيح (boolean, number, string)
- ✅ التطبيق ينتظر جاهزية قاعدة البيانات (تأخير 500ms)
- ✅ تم اختبار تحويل الأنواع وجميع الاختبارات نجحت
- ⚠️ إذا فشل استرجاع الإعدادات من قاعدة البيانات، سيتم استخدام القيم الافتراضية

---

## 🚀 خطوات ما بعد الاختبار

بعد التحقق من نجاح الاختبار:
1. جرّب الطباعة الفعلية بطابعة حرارية
2. تحقق من أن الإعدادات تُستخدم في الطباعة بشكل صحيح
3. اختبر مع إعادات تشغيل متعددة

---

**آخر تحديث**: الآن ✅
**الحالة**: مستعد للاختبار 🧪
