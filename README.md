# 🧾 تصفية برو - Tasfiya Pro v4.0.0

نظام تسوية نقدية احترافي مع دعم كامل للغة العربية وواجهات RTL.

## ✨ المميزات الرئيسية

- ✅ إدارة التسويات بين المبيعات والإيصالات
- ✅ تتبع معاملات البنك والنقد والمبيعات بالآجل
- ✅ واجهة عربية كاملة (RTL)
- ✅ طباعة وتصدير PDF
- ✅ نسخ احتياطي تلقائي للبيانات
- ✅ قاعدة بيانات SQLite آمنة
- ✅ دعم البحث والتصفية المتقدم

## 🚀 الاستخدام السريع

### المتطلبات
- Windows 10/11
- Node.js 16+
- npm 8+

### تثبيت وبناء التطبيق

```bash
# تثبيت المتطلبات
npm install

# بناء التطبيق نظيف
npm run build

# أو استخدم سكريبت البناء الشامل
npm run build-clean
```

### تشغيل التطبيق

```bash
# تشغيل الإصدار المثبت
npm start

# أو في وضع التطوير
npm run dev
```

## 📁 هيكل المشروع

```
casher/
├── src/                      # كود التطبيق
│   ├── main.js              # معالج Electron الرئيسي
│   ├── app.js               # منطق التطبيق الأساسي
│   ├── index.html           # الواجهة الرئيسية
│   ├── styles.css           # الأنماط CSS
│   ├── database.js          # إدارة قاعدة البيانات
│   ├── pdf-generator.js     # توليد ملفات PDF
│   └── components/          # مكونات الواجهة
├── assets/                  # الموارد (الأيقونات، الخطوط)
├── dist/                    # مجلد البناء (ملفات التنفيذ)
├── package.json             # إعدادات المشروع
├── build.js                 # سكريبت البناء
├── electron-builder.config.js # إعدادات بناء Electron
└── README.md                # هذا الملف
```

## 🔧 أوامر البناء المتاحة

```bash
npm start              # تشغيل التطبيق
npm run dev           # وضع التطوير
npm run build         # بناء الإصدار الإنتاجي
npm run build-win     # بناء لـ Windows
npm run build-clean   # بناء شامل نظيف (مع تنظيف)
```

## 🏗️ عملية البناء النظيفة

تم تنظيف وتبسيط عملية البناء بالكامل:

### الخطوات:
1. **تنظيف البنايات القديمة** ✓
2. **تثبيت المتطلبات** ✓
3. **بناء التطبيق** ✓
4. **التحقق من النتائج** ✓

### الملفات المحذوفة:
- ❌ ملفات إعدادات البناء المتعددة
- ❌ التقارير والتوثيق القديم
- ❌ الملفات الاختبارية المؤقتة
- ❌ النسخ الاحتياطية غير المستخدمة

### الملفات الباقية:
- ✅ `electron-builder.config.js` - إعداد البناء الموحد
- ✅ `build.js` - سكريبت البناء النظيف
- ✅ `package.json` - إعدادات مبسطة

## 📊 معلومات الإصدار

- **رقم الإصدار**: 4.0.0
- **نوع البناء**: Production Release
- **المنصة المستهدفة**: Windows 10/11 (x64)
- **صيغ التوزيع**: 
  - NSIS Installer (تثبيت كامل)
  - Portable EXE (نسخة محمولة)
- **حجم التطبيق**: ~200-250 MB (مضغوط)
- **التاريخ**: 14 نوفمبر 2025

## 💾 قاعدة البيانات

- **نوع**: SQLite3 (better-sqlite3)
- **الموقع**: AppData محلي (آمن)
- **النسخ الاحتياطية**: تلقائياً يومياً
- **الجداول الأساسية**:
  - reconciliations (التسويات)
  - bank_receipts (الإيصالات البنكية)
  - cash_receipts (الإيصالات النقدية)
  - postpaid_sales (المبيعات بالآجل)
  - customer_receipts (استقبالات العملاء)

## 🎨 التصميم والتقنيات

- **الواجهة**: HTML5 + CSS3
- **الإطار**: Bootstrap 5
- **اللغات**: JavaScript ES6+
- **قاعدة البيانات**: SQLite3
- **البناء**: electron-builder
- **PDF**: Puppeteer
- **دعم العربية**: كامل مع RTL

## 👤 بيانات الدخول الافتراضية

| الحقل | القيمة |
|------|--------|
| اسم المستخدم | admin |
| كلمة المرور | admin123 |

⚠️ **تنبيه أمني**: غيّر كلمة المرور بعد أول تسجيل دخول!

## 🐛 الحل السريع للمشاكل

### البناء يفشل:
```bash
# حذف المكتبات وإعادة التثبيت
rm -r node_modules
npm install
npm run build
```

### مشاكل قاعدة البيانات:
- تحقق من صلاحيات الكتابة على المجلد
- أعد تشغيل التطبيق
- تحقق من وجود ملف قاعدة البيانات

### مشاكل الطباعة:
- تأكد من تثبيت Puppeteer صحيحاً
- تحقق من إعدادات النظام
- أعد تثبيت المكتبات

## 📞 الدعم والمساعدة

1. تحقق من قسم الحل السريع أعلاه
2. استعرض السجلات في وحدة المطور (F12)
3. تأكد من تثبيت المتطلبات بشكل صحيح

## 📄 الترخيص

© 2025 محمد أمين الكامل - جميع الحقوق محفوظة

⚖️ يمنع الاستخدام أو التعديل دون إذن كتابي صريح

---

**الحالة**: جاهز للإنتاج ✅
**آخر تحديث**: 14 نوفمبر 2025
